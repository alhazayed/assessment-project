<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config — enforces HTTPS for all connections.
  Certificate pinning will be added in Phase 4 (Security Hardening).
-->
<network-security-config>
    <!-- No cleartext (http://) traffic allowed -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Production server -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">vwelfare.vercel.app</domain>
        <domain includeSubdomains="true">supabase.co</domain>
    </domain-config>

    <!-- Development only — allow localhost for debugging -->
    <!-- Remove this block in production builds -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
